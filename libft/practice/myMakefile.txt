CC = gcc
CFLAGS = -Wall -Wextra -Werror
RM = rm -f

FILE =	ft_isalpha \
		ft_isalnum

SRCSDIR = ./source

SRCS = (addprefix $(SRCSDIR), $(addsuffix .c, $(FILE)))

all = $(FILE)

$(FILE) : % : %.c
		$(CC) $(CFLAGS) -o $@ $<

#$(FILE).c : %.c : libft.h

norm :
	norminette $(FILE)

clean :
	$(RM) $(FILE)

norm :
	norminette $(FILE)
